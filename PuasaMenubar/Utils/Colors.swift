import SwiftUI

extension Color {
    // Ramadan green theme color
    static let ramadanGreen = Color(red: 0.0, green: 0.6, blue: 0.4)
    
    // Light green for backgrounds
    static let ramadanGreenLight = Color(red: 0.0, green: 0.6, blue: 0.4).opacity(0.1)
    
    // Darker green for accents
    static let ramadanGreenDark = Color(red: 0.0, green: 0.5, blue: 0.35)
}
